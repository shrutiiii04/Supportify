<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/nav.css">
    <script src="https://kit.fontawesome.com/584906d3aa.js" crossorigin="anonymous"></script>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-left">
            <i class="fa-regular fa-compass icon"></i>
            <div class="navbar-brand">
                <a href="/">Supportify</a>
            </div>
        </div>
        <div class="navbar-middle">
            <ul class="navbar-block">
                <li class="item"><a href="/listings">Explore</a></li>
                <li class="item"><a href="/listings/new">Add New Property</a></li>
            </ul>
        </div>
        <div class="profile-info">
            <div class="profile-name">
                <% if(currUser) { %>
                <span><%= currUser.username %></span>
                <% } else { %>
                <span>Profile</span>
                <% } %>
                <i class="fa-solid fa-user"></i>
            </div>
            <ul class="dropdown-menu">
                <% if(!currUser) { %>
                <li class="item"><a href="/signup">Sign In</a></li>
                <li class="item"><a href="/login">Login</a></li>
                <% } %>
                <% if(currUser) { %>
                <li class="item"><a href="/logout">Logout</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
</body>
</html>
